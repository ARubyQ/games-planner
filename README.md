# Планировщик игр НРИ

Веб-приложение для планирования времени игр в настольные ролевые игры (НРИ).

## Возможности

- 📅 Календарь на 3 недели с почасовыми слотами
- 👥 Множественные участники с выбором времени
- 🌍 Выбор часового пояса (UTC-4 до UTC+4)
- 📱 Адаптивный дизайн для мобильных устройств
- 🎯 Подсветка слотов выбранных участников
- 🔄 Синхронизация в реальном времени

## Быстрый старт

### Локальная разработка

```bash
# Установить зависимости
npm install

# Запустить в режиме разработки (фронтенд + бэкенд)
npm run dev:full
```

Приложение будет доступно:
- Фронтенд: http://localhost:5173
- Бэкенд API: http://localhost:3001

### Развертывание через Docker

#### Вариант 1: Docker Compose

```bash
# Собрать и запустить
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить
docker-compose down
```

#### Вариант 2: Portainer (Stack)

1. В Portainer перейдите в **Stacks** → **Add stack**
2. Вставьте содержимое `docker-compose.yml`
3. Укажите имя стека (например, `games-planner`)
4. Нажмите **Deploy the stack**

#### Вариант 3: Portainer (Git Repository) - Рекомендуется

1. В Portainer перейдите в **Stacks** → **Add stack**
2. Выберите **Repository**
3. Укажите:
   - **Repository URL**: URL вашего Git репозитория (например, `https://github.com/username/games-planner.git`)
   - **Compose path**: `docker-compose.yml` (или оставьте пустым, если файл в корне)
   - **Stack name**: `games-planner` (или любое другое имя)
4. Нажмите **Deploy the stack**

**Примечание**: Portainer автоматически соберет образ из Dockerfile при первом развертывании. При обновлении стека образ будет пересобран автоматически.

#### Вариант 4: Прямой Docker

```bash
# Собрать образ
docker build -t games-planner .

# Запустить контейнер
docker run -d \
  --name games-planner \
  -p 3001:3001 \
  -v games-planner-data:/app/data \
  --restart unless-stopped \
  games-planner
```

## Конфигурация

### Переменные окружения

- `PORT` - Порт для сервера (по умолчанию: 3001)
- `NODE_ENV` - Режим работы (production/development)
- `EXTERNAL_PORT` - Внешний порт для доступа (по умолчанию: 3001)
- `BASE_URL` - Базовый URL приложения (опционально, для работы с доменом)

### Работа с доменом

#### Вариант 1: Прямой доступ (без reverse proxy)

Если приложение доступно напрямую по домену:
- Просто укажите нужный порт в `docker-compose.yml` или Portainer
- Приложение будет доступно по `http://your-domain.com:3001`

#### Вариант 2: С Nginx reverse proxy (рекомендуется)

1. Используйте `docker-compose.proxy.yml` вместо `docker-compose.yml`
2. Настройте `nginx.conf` с вашим доменом
3. Настройте SSL сертификаты (Let's Encrypt через certbot)
4. Приложение будет доступно по `https://your-domain.com`

### Персистентность данных

Данные сохраняются в volume `games-planner-data`. При пересоздании контейнера данные сохраняются.

## Использование

1. Откройте приложение в браузере
2. Введите ваш никнейм
3. Выберите удобное время на календаре
4. Другие участники увидят ваш выбор в реальном времени

### Множественные календари

Доступ к разным календарям через URL:
- `/` - календарь по умолчанию
- `/fandalin` - календарь "fandalin"
- `/campaign-name` - любой другой календарь

## Технологии

- **Frontend**: React, Vite
- **Backend**: Node.js, Express
- **Стилизация**: CSS
- **Контейнеризация**: Docker

## Лицензия

MIT
